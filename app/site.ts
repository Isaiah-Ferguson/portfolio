/**
 * Canonical site URL — used by metadata, sitemap, and robots.
 * Update this once when the real domain is connected
 * (currently the expected Vercel deployment URL).
 */
export const SITE_URL = "https://isaiah-ferguson.vercel.app";
